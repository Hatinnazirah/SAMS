import 'package:flutter/material.dart';

class PusatAdabCurriculumPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Curriculum Management')),
      body: Center(child: Text('Pusat Adab Curriculum Page')),
    );
  }
}