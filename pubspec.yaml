name: sams_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  
  # UI & Icons
  cupertino_icons: ^1.0.8
  
  # Image Picker - For selecting images from gallery/camera
  image_picker: ^1.0.4
<<<<<<< Updated upstream

=======
  
  # Firebase Core - Required for all Firebase services
  firebase_core: ^4.10.0
  
  # Cloud Firestore - For database operations
  cloud_firestore: ^6.5.0
  
  # Firebase Storage - For file uploads/downloads
  firebase_storage: 13.4.2
  
  # File Picker - For selecting files from device
  file_picker: ^8.3.4
  
  # Intl - For date formatting
  intl: ^0.19.0
  
  # PDF Viewer - Use one of these options (choose ONE):
  # Option 1: flutter_pdfview (most popular)
  flutter_pdfview: ^1.3.1
  
  # Option 2: OR use pdfx
  # pdfx: ^2.5.0
  
  # Option 3: OR use advance_pdf_viewer
  # advance_pdf_viewer: ^2.0.1
  
  # Web View (for displaying documents)
  webview_flutter: ^4.10.0
>>>>>>> Stashed changes

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/icons/ModuleBookingIcon.png
    - assets/icons/MySubjectsIcon.png
    - assets/icons/ActivitiesIcon.png
    - assets/icons/CreditClaimIcon.png
    - assets/icons/sams_logo.png
    - assets/icons/PusatAdabIcon1.png
    - assets/icons/PusatAdabIcon2.png